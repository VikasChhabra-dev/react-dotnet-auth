# React Auth UI (Vite)

Frontend for the ASP.NET Core JWT + Refresh Token Rotation + Device Tracking demo.

## Run

```bash
npm install
npm run dev
```

App runs on:
- http://localhost:5173

## Notes
- Google ClientId placeholder is inside `src/main.jsx`
- Facebook AppId placeholder is inside `src/pages/Login.jsx`
